<div class="container col-6">
  <h1>Edit <%= @dog.name %>'s Medical Record</h1>
  <%= link_to "Go back", dog_medical_records_path(@dog) %>
  <%= simple_form_for(@medical_record) do |f| %>
    <p>Date<%= f.input :date, as: :string, input_html: { data: { controller: "datepicker" } }, label: false %></a></p>
    <%= f.input :description %>
    <%= f.input :vet_contact %>
    <div class="form-actions">
      <%= f.button :submit, class:"btn btn-info my-3" %>
    </div>
  <% end %>
</div>
