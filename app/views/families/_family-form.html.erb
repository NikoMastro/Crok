<%= simple_form_for current_user.family do |f| %>
  <%= f.input :name %>
  <!-- User shouldnt be able to validate if form is blank-->
  <%= f.submit "Submit", class:"btn btn-info my-3" %>
  <!-- When user validates, the user should stay on the same page-->
  <!-- Display a message saying "Your family name has been entered as #{family.name}-->
<% end %>
