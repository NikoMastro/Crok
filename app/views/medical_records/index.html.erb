
<div class="medical-img">
  <%= image_tag 'banner_vet_01.jpg', alt: 'Banner Image'%>
</div>
<div class="container col-12 col-md-6 justify-content-center align-items-center">
  <div style="background-color: #ffc65a; padding: 10px; margin-bottom: 10px; margin-top: 10px;">
    <h3><%= @dog.name %>'s Medical Records</h3>
    <%= link_to "Add new record", "medical_records/new", class: "btn my-btn-green" %>
    <%= link_to "Fitness History", :dog_id_health, class: "btn my-btn-green" %>

  </div>
</div>
<div class="container col-md-6 justify-content-center align-items-center">
  <div class="last-updates">
    <div class="last-update">
      <% if @medical_records.empty? %>
        <p>No medical records added</p>
      <% else %>
        <% @medical_records.each_slice(2) do |slice| %>
          <div class="col-12 mb-3">
            <div class="row">
              <% slice.each do |record| %>
                <div class="col-md-6 col-12">
                  <div class="health-card">
                    <%= link_to dog_medical_record_path(record.dog, record) do %>
                      <%= image_tag 'dog_med_banner_01.svg', alt: 'Image phone', class: 'small-card' %>
                    <% end %>
                    <p><strong>Date created: </strong><%= record.date.strftime("%B %d, %Y") %></p>
                    <p><strong>Description: </strong><%= record.description %></p>
                    <p><strong>Veterinarian: </strong><%= record.vet_contact %></p>
                    <div class="card-buttons">
                      <%= link_to "Edit", edit_medical_record_path(record), class: "btn btn-outline-primary" %>
                      <%= link_to "Delete", medical_record_path(record), class: "btn btn-outline-primary", data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this entry?" } %>
                    </div>
                  </div>
                </div>
              <% end %>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
<div class="container">
  <div class="row">
    <div class="col-md-6 d-flex justify-content-center align-items-center">
      <div class="graph">
      </div>
    </div>
  </div>
</div>
<br>
<%= render 'shared/bottom_nav' %>
